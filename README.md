# motion

Vue + Vite Chrome extension that overrides the new tab page.

## Setup

1. Install dependencies with `npm install`.
2. Run `npm run build` to generate the extension bundle in `dist/`.
3. Load the `dist/` folder as an unpacked extension in Chrome.

## Notes

- The extension uses Manifest V3.

